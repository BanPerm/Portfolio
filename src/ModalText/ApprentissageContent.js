import React from "react";

const ApprentissageContent = ({titre, texte}) => {
    return (
        <div>
            <h5>{titre}</h5>
            <p>{texte}</p>
        </div>
    );
};

export default ApprentissageContent;